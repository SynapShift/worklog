# WorkLog does not require runtime environment variables for local web usage.
#
# Cloudflare Pages Functions need a D1 binding named DB.
# Copy wrangler.example.toml to wrangler.toml and fill in your own database_id.
